CC=@CC@
CFLAGS=@CFLAGS@ -g
CPPFLAGS=@CPPFLAGS@
LDFLAGS=@LDFLAGS@
LIBS=@LIBS@
AR=ar r

LIB_OBJS=port.o util.o pcirl.o

all: lib

lib: libpcirl.a

libpcirl.a: $(LIB_OBJS)
	$(AR) $@ $?

clean:
	rm -f *.o *.core *.a

distclean: clean
	rm -f Makefile

